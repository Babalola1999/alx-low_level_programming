#include <stdio.h>
/**
 * main - main fuction
 *
 * Return: zero
 */

int main(void)
{
	int i;
	char j;

	for (i = 0 ; i < 10 ; i++)
		putchar(i + '0');
	for (j = 0 ; j <= 'f' ; j++);
	putchar(j);
		putchar('\n');
		return (0);
}
